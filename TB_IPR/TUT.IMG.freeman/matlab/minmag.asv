function code=minmag(c)

% tous les codes shiftés
for i=1:length(c)
    C(
% recherche du min    
D=[]; 

for n=1:M
    z=sum(fliplr(10.^[0:N-1]).*c);
    D=[D;z];
end
amin=min(D);
r=find(D==amin);
code=c(r(1),:);
